CowrieLogs
| where eventid == "cowrie.login.success"
| where (username == "root" and password == "toor")
    or (username == "admin" and password == "admin")
| project timestamp, src_ip, username, password, session, protocol
| sort by timestamp asc
